import React from 'react';
import { Card, CardMedia } from '@material-ui/core';

const CardHero = () => {
  return (
    <Card>
      <CardMedia
        component="img"
        alt={`imagem do super ${data.name}`}
        image={data.images.md}
      >
      
      </CardMedia>
    </Card>
  );
}